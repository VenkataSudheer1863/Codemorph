<?xml version="1.0" encoding="UTF-8"?>
<persistence version="2.2"
             xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd">

    <persistence-unit name="universityPU" transaction-type="JTA">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- JTA DataSource configured in WildFly -->
        <jta-data-source>java:jboss/datasources/UniversityDS</jta-data-source>

        <!-- Entity Classes -->
        <class>com.university.persistence.entity.Student</class>
        <class>com.university.persistence.entity.Faculty</class>
        <class>com.university.persistence.entity.Course</class>
        <class>com.university.persistence.entity.Enrollment</class>
        <class>com.university.persistence.entity.Exam</class>
        <class>com.university.persistence.entity.Result</class>

        <exclude-unlisted-classes>false</exclude-unlisted-classes>

        <properties>
            <!-- Auto-create / update schema -->
            <property name="javax.persistence.schema-generation.database.action" value="create"/>

            <!-- Hibernate dialect for PostgreSQL -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQL10Dialect"/>

            <!-- Show SQL in logs (useful for debugging) -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>

            <!-- Connection pool settings (managed by WildFly) -->
            <property name="hibernate.transaction.jta.platform"
                      value="org.hibernate.engine.transaction.jta.platform.internal.JBossAppServerJtaPlatform"/>
        </properties>
    </persistence-unit>
</persistence>
